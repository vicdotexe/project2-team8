<section class="flex flex-col justify-around items-center w-80 h-96 bg-white rounded-xl m-3 p-3">
    <div class="h-1/2 border border-black w-full">
      <a href="/artPiece/{{id}}" >
      <img class="h-full" src="{{path}}" alt="{{description}}">
      </a>
    </div>

    <div id="h-1/2">
      <p>{{name}}</p>
      <a href="/gallery/{{User.id}}">
      <p>By {{User.username}}</p> </a> Created: {{createdAt}} </p>
    </div>
</section>