<section class="flex flex-col justify-around items-center w-80 h-96 bg-black rounded-xl m-3 p-3">
    <div class="h-3/4 border border-black w-full">
      <a href="/artPiece/{{id}}" >
      <img class="aspect-auto w-full h-full" src="{{path}}" alt="{{description}}">
      </a>
    </div>

    <div id="h-1/2">
      <h5 class="font-bold">{{name}}</p>
      <h6>- <a href="/gallery/{{User.id}}">{{User.username}}</a></h6>
    </div>
</section>