<button id="watchButton" class="bg-sky-500 rounded p-2" data-id="{{galleryId}}">
    {{#if isWatching}}
    Unwatch
    {{else}}
    Watch
    {{/if}}
</button>
<script src="/js/watchButton.js"></script>